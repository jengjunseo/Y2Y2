{
  "name": "y2y2",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "engines": { "node": "24.x" },
  "scripts": {
    "check:web": "node --check public/engine-client.js && node --check public/app.js && node --check public/sw.js",
    "check": "npm run check:web"
  }
}
