{
  "name": "y2y2",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check:worker": "node --check worker/index.js",
    "check:container": "python3 -m py_compile container/server.py",
    "check": "npm run check:worker && npm run check:container"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  },
  "devDependencies": {
    "wrangler": "^4.118.0"
  }
}
