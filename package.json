{
  "name": "y2y2",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "check": "node --check api/health.js && node --check api/inspect.js && node --check api/jobs.js && node --check api/job.js && node --check api/history.js && node --check api/download.js && node --check lib/sandbox.js && node --check lib/http.js && python3 -m py_compile sandbox/backend.py"
  },
  "dependencies": {
    "@vercel/sandbox": "^1.0.2"
  }
}
